import testSuiteUtil = require("./utils");


describe('Fixed Actions Test Set',function(){

    describe('Open declaration tests',function(){

        it("test001/api.raml", function (done) {
            this.timeout(15000);
            testSuiteUtil.testOpenDeclaration("/test001.raml", done);
        });

        it("test002/api.raml", function (done) {
            this.timeout(15000);
            testSuiteUtil.testOpenDeclaration("/test002.raml", done);
        });

        it("test003/api.raml", function (done) {
            this.timeout(15000);
            testSuiteUtil.testOpenDeclaration("/test003.raml", done);
        });

        it("test004/api.raml", function (done) {
            this.timeout(15000);
            testSuiteUtil.testOpenDeclaration("/test004.raml", done);
        });

        it("test005/api.raml", function (done) {
            this.timeout(15000);
            testSuiteUtil.testOpenDeclaration("/test005.raml", done);
        });

        it("test006/api.raml", function (done) {
            this.timeout(15000);
            testSuiteUtil.testOpenDeclaration("/test006.raml", done);
        });

        it("test007/api.raml", function (done) {
            this.timeout(15000);
            testSuiteUtil.testOpenDeclaration("/test007.raml", done);
        });

        it("test008/api.raml", function (done) {
            this.timeout(15000);
            testSuiteUtil.testOpenDeclaration("/test008.raml", done);
        });

        it("test009/api.raml", function (done) {
            this.timeout(15000);
            testSuiteUtil.testOpenDeclaration("/test009.raml", done);
        });

        it("test010/api.raml", function (done) {
            this.timeout(15000);
            testSuiteUtil.testOpenDeclaration("/test010.raml", done);
        });

        it("test011/api.raml", function (done) {
            this.timeout(15000);
            testSuiteUtil.testOpenDeclaration("/test011.raml", done);
        });

        it("test012/api.raml", function (done) {
            this.timeout(15000);
            testSuiteUtil.testOpenDeclaration("/test012.raml", done);
        });

        it("test013/api.raml", function (done) {
            this.timeout(15000);
            testSuiteUtil.testOpenDeclaration("/test013.raml", done);
        });

        it("test014/api.raml", function (done) {
            this.timeout(15000);
            testSuiteUtil.testOpenDeclaration("/test014.raml", done);
        });

        it("test015/api.raml", function (done) {
            this.timeout(15000);
            testSuiteUtil.testOpenDeclaration("/test015.raml", done);
        });

        it("test016/api.raml", function (done) {
            this.timeout(15000);
            testSuiteUtil.testOpenDeclaration("/test016.raml", done);
        });

        it("test017/api.raml", function (done) {
            this.timeout(15000);
            testSuiteUtil.testOpenDeclaration("/test017.raml", done);
        });

        it("test018/api.raml", function (done) {
            this.timeout(15000);
            testSuiteUtil.testOpenDeclaration("/test018.raml", done);
        });

        it("test019/api.raml", function (done) {
            this.timeout(15000);
            testSuiteUtil.testOpenDeclaration("/test019.raml", done);
        });

        it("test020/api.raml", function (done) {
            this.timeout(15000);
            testSuiteUtil.testOpenDeclaration("/test020.raml", done);
        });

        it("test021/api.raml", function (done) {
            this.timeout(15000);
            testSuiteUtil.testOpenDeclaration("/test021.raml", done);
        });

        it("test022/api.raml", function (done) {
            this.timeout(15000);
            testSuiteUtil.testOpenDeclaration("/test022.raml", done);
        });

        it("test023/api.raml", function (done) {
            this.timeout(15000);
            testSuiteUtil.testOpenDeclaration("/test023.raml", done);
        });

        it("test024/api.raml", function (done) {
            this.timeout(15000);
            testSuiteUtil.testOpenDeclaration("/test024.raml", done);
        });

        it("test025/api.raml", function (done) {
            this.timeout(15000);
            testSuiteUtil.testOpenDeclaration("/test025.raml", done);
        });

        it("test026/api.raml", function (done) {
            this.timeout(15000);
            testSuiteUtil.testOpenDeclaration("/test026.raml", done);
        });

        it("test027/api.raml", function (done) {
            this.timeout(15000);
            testSuiteUtil.testOpenDeclaration("/test027.raml", done);
        });

        it("test028/api.raml", function (done) {
            this.timeout(15000);
            testSuiteUtil.testOpenDeclaration("/test028.raml", done);
        });

        it("test029/api.raml", function (done) {
            this.timeout(15000);
            testSuiteUtil.testOpenDeclaration("/test029.raml", done);
        });

        it("test030/api.raml", function (done) {
            this.timeout(15000);
            testSuiteUtil.testOpenDeclaration("/test030.raml", done);
        });
/* //TODO uncomment after fixing https://www.mulesoft.org/jira/browse/ALS-230
        it("test031/api.raml", function (done) {
         this.timeout(15000);
         testSuiteUtil.testOpenDeclaration("/test031.raml", done);
        });
*/
        it("test032/api.raml", function (done) {
            this.timeout(15000);
            testSuiteUtil.testOpenDeclaration("/test032.raml", done);
        });
/* //TODO uncomment after fixing https://www.mulesoft.org/jira/browse/ALS-230
        it("test033/api.raml", function (done) {
         this.timeout(15000);
         testSuiteUtil.testOpenDeclaration("/test033.raml", done);
        });
*/
        it("test034/api.raml", function (done) {
            this.timeout(15000);
            testSuiteUtil.testOpenDeclaration("/test034.raml", done);
        });

        it("test035/api.raml", function (done) {
            this.timeout(15000);
            testSuiteUtil.testOpenDeclaration("/test035.raml", done);
        });

        it("test036/api.raml", function (done) {
            this.timeout(15000);
            testSuiteUtil.testOpenDeclaration("/test036.raml", done);
        });

        it("test037/api.raml", function (done) {
            this.timeout(15000);
            testSuiteUtil.testOpenDeclaration("/test037.raml", done);
        });

        it("test038/api.raml", function (done) {
            this.timeout(15000);
            testSuiteUtil.testOpenDeclaration("/test038.raml", done);
        });

        it("test039/api.raml", function (done) {
            this.timeout(15000);
            testSuiteUtil.testOpenDeclaration("/test039.raml", done);
        });

        it("test040/api.raml", function (done) {
            this.timeout(15000);
            testSuiteUtil.testOpenDeclaration("/test040.raml", done);
        });

        it("test041/api.raml", function (done) {
            this.timeout(15000);
            testSuiteUtil.testOpenDeclaration("/test041.raml", done);
        });

        it("test041/api.raml", function (done) {
            this.timeout(15000);
            testSuiteUtil.testOpenDeclaration("/test041.raml", done);
        });

        it("test042/api.raml", function (done) {
            this.timeout(15000);
            testSuiteUtil.testOpenDeclaration("/test042.raml", done);
        });

        it("test043/api.raml", function (done) {
            this.timeout(15000);
            testSuiteUtil.testOpenDeclaration("/test043.raml", done);
        });

        it("test044/api.raml", function (done) {
            this.timeout(15000);
            testSuiteUtil.testOpenDeclaration("/test044.raml", done);
        });

        it("test045/api.raml", function (done) {
            this.timeout(15000);
            testSuiteUtil.testOpenDeclaration("/test045.raml", done);
        });
/* //TODO uncomment after fixing https://www.mulesoft.org/jira/browse/ALS-230
        it("test046/api.raml", function (done) {
         this.timeout(15000);
         testSuiteUtil.testOpenDeclaration("/test046.raml", done);
        });
*/
        it("test047/api.raml", function (done) {
            this.timeout(15000);
            testSuiteUtil.testOpenDeclaration("/test047.raml", done);
        });

        it("test048/api.raml", function (done) {
            this.timeout(15000);
            testSuiteUtil.testOpenDeclaration("/test048.raml", done);
        });

        it("test049/api.raml", function (done) {
            this.timeout(15000);
            testSuiteUtil.testOpenDeclaration("/test049.raml", done);
        });
    });

    describe('Find references tests',function(){

        it("test001/api.raml", function (done) {
            this.timeout(15000);
            testSuiteUtil.testFindReferences("/test001.raml", done);
        });

        it("test002/api.raml", function (done) {
            this.timeout(15000);
            testSuiteUtil.testFindReferences("/test002.raml", done);
        });

        it("test003/api.raml", function (done) {
            this.timeout(15000);
            testSuiteUtil.testFindReferences("/test003.raml", done);
        });

        it("test004/api.raml", function (done) {
            this.timeout(15000);
            testSuiteUtil.testFindReferences("/test004.raml", done);
        });

        it("test005/api.raml", function (done) {
            this.timeout(15000);
            testSuiteUtil.testFindReferences("/test005.raml", done);
        });

        it("test006/api.raml", function (done) {
            this.timeout(15000);
            testSuiteUtil.testFindReferences("/test006.raml", done);
        });

        it("test007/api.raml", function (done) {
            this.timeout(15000);
            testSuiteUtil.testFindReferences("/test007.raml", done);
        });

        // TODO uncomment when https://www.mulesoft.org/jira/browse/ALS-220 is fixed.
        // it("test008/api.raml", function (done) {
        //     this.timeout(15000);
        //     testSuiteUtil.testFindReferences("/test008.raml", done);
        // });
        //
        // it("test009/api.raml", function (done) {
        //     this.timeout(15000);
        //     testSuiteUtil.testFindReferences("/test009.raml", done);
        // });

        it("test010/api.raml", function (done) {
            this.timeout(15000);
            testSuiteUtil.testFindReferences("/test010.raml", done);
        });

        // TODO uncomment when https://www.mulesoft.org/jira/browse/ALS-221 is fixed.
        // it("test011/api.raml", function (done) {
        //     this.timeout(15000);
        //     testSuiteUtil.testFindReferences("/test011.raml", done);
        // });
        //
        // it("test012/api.raml", function (done) {
        //     this.timeout(15000);
        //     testSuiteUtil.testFindReferences("/test012.raml", done);
        // });
        //
        // it("test013/api.raml", function (done) {
        //     this.timeout(15000);
        //     testSuiteUtil.testFindReferences("/test013.raml", done);
        // });
        //
        // it("test014/api.raml", function (done) {
        //     this.timeout(15000);
        //     testSuiteUtil.testFindReferences("/test014.raml", done);
        // });

        it("test015/api.raml", function (done) {
            this.timeout(15000);
            testSuiteUtil.testFindReferences("/test015.raml", done);
        });

        it("test016/api.raml", function (done) {
            this.timeout(15000);
            testSuiteUtil.testFindReferences("/test016.raml", done);
        });

        it("test017/api.raml", function (done) {
            this.timeout(15000);
            testSuiteUtil.testFindReferences("/test017.raml", done);
        });

        it("test018/api.raml", function (done) {
            this.timeout(15000);
            testSuiteUtil.testFindReferences("/test018.raml", done);
        });

        it("test019/api.raml", function (done) {
            this.timeout(15000);
            testSuiteUtil.testFindReferences("/test019.raml", done);
        });

        it("test020/api.raml", function (done) {
            this.timeout(15000);
            testSuiteUtil.testFindReferences("/test020.raml", done);
        });

        it("test021/api.raml", function (done) {
            this.timeout(15000);
            testSuiteUtil.testFindReferences("/test021.raml", done);
        });

        it("test022/api.raml", function (done) {
            this.timeout(15000);
            testSuiteUtil.testFindReferences("/test022.raml", done);
        });

        it("test023/api.raml", function (done) {
            this.timeout(15000);
            testSuiteUtil.testFindReferences("/test023.raml", done);
        });

        it("test024/api.raml", function (done) {
            this.timeout(15000);
            testSuiteUtil.testFindReferences("/test024.raml", done);
        });

        it("test025/api.raml", function (done) {
            this.timeout(15000);
            testSuiteUtil.testFindReferences("/test025.raml", done);
        });

        it("test026/api.raml", function (done) {
            this.timeout(15000);
            testSuiteUtil.testFindReferences("/test026.raml", done);
        });

        it("test027/api.raml", function (done) {
            this.timeout(15000);
            testSuiteUtil.testFindReferences("/test027.raml", done);
        });

        it("test028/api.raml", function (done) {
            this.timeout(15000);
            testSuiteUtil.testFindReferences("/test028.raml", done);
        });

        it("test029/api.raml", function (done) {
            this.timeout(15000);
            testSuiteUtil.testFindReferences("/test029.raml", done);
        });

        it("test030/api.raml", function (done) {
            this.timeout(15000);
            testSuiteUtil.testFindReferences("/test030.raml", done);
        });

        it("test031/api.raml", function (done) {
            this.timeout(15000);
            testSuiteUtil.testFindReferences("/test031.raml", done);
        });

        it("test032/api.raml", function (done) {
            this.timeout(15000);
            testSuiteUtil.testFindReferences("/test032.raml", done);
        });

        it("test033/api.raml", function (done) {
            this.timeout(15000);
            testSuiteUtil.testFindReferences("/test033.raml", done);
        });

        it("test034/api.raml", function (done) {
            this.timeout(15000);
            testSuiteUtil.testFindReferences("/test034.raml", done);
        });
/* // TODO uncomment when https://www.mulesoft.org/jira/browse/ALS-220 is fixed.
        it("test035/api.raml", function (done) {
         this.timeout(15000);
         testSuiteUtil.testFindReferences("/test035.raml", done);
        });
*/
        it("test036/api.raml", function (done) {
            this.timeout(15000);
            testSuiteUtil.testFindReferences("/test036.raml", done);
        });

        it("test037/api.raml", function (done) {
            this.timeout(15000);
            testSuiteUtil.testFindReferences("/test037.raml", done);
        });

        it("test038/api.raml", function (done) {
            this.timeout(15000);
            testSuiteUtil.testFindReferences("/test038.raml", done);
        });

        it("test039/api.raml", function (done) {
            this.timeout(15000);
            testSuiteUtil.testFindReferences("/test039.raml", done);
        });

        it("test040/api.raml", function (done) {
            this.timeout(15000);
            testSuiteUtil.testFindReferences("/test040.raml", done);
        });

        it("test041/api.raml", function (done) {
            this.timeout(15000);
            testSuiteUtil.testFindReferences("/test041.raml", done);
        });

        it("test041/api.raml", function (done) {
            this.timeout(15000);
            testSuiteUtil.testFindReferences("/test041.raml", done);
        });

        it("test042/api.raml", function (done) {
            this.timeout(15000);
            testSuiteUtil.testFindReferences("/test042.raml", done);
        });

        it("test043/api.raml", function (done) {
            this.timeout(15000);
            testSuiteUtil.testFindReferences("/test043.raml", done);
        });

/* //TODO uncomment after fixing https://www.mulesoft.org/jira/browse/ALS-228
        it("test044/api.raml", function (done) {
         this.timeout(15000);
         testSuiteUtil.testFindReferences("/test044.raml", done);
        });
*/
        it("test045/api.raml", function (done) {
            this.timeout(15000);
            testSuiteUtil.testFindReferences("/test045.raml", done);
        });
/* //TODO uncomment after fixing https://www.mulesoft.org/jira/browse/ALS-231
        it("test046/api.raml", function (done) {
         this.timeout(15000);
         testSuiteUtil.testFindReferences("/test046.raml", done);
        });
*/
        it("test047/api.raml", function (done) {
            this.timeout(15000);
            testSuiteUtil.testFindReferences("/test047.raml", done);
        });

        it("test048/api.raml", function (done) {
            this.timeout(15000);
            testSuiteUtil.testFindReferences("/test048.raml", done);
        });

        it("test049/api.raml", function (done) {
            this.timeout(15000);
            testSuiteUtil.testFindReferences("/test049.raml", done);
        });
    });

    describe('Mark occurrences tests',function(){

        it("test001/api.raml", function (done) {
            this.timeout(15000);
            testSuiteUtil.testMarkOccurrences("/test001.raml", done);
        });

        it("test002/api.raml", function (done) {
            this.timeout(15000);
            testSuiteUtil.testMarkOccurrences("/test002.raml", done);
        });

        it("test003/api.raml", function (done) {
            this.timeout(15000);
            testSuiteUtil.testMarkOccurrences("/test003.raml", done);
        });

        it("test004/api.raml", function (done) {
            this.timeout(15000);
            testSuiteUtil.testMarkOccurrences("/test004.raml", done);
        });

        it("test005/api.raml", function (done) {
            this.timeout(15000);
            testSuiteUtil.testMarkOccurrences("/test005.raml", done);
        });

        it("test006/api.raml", function (done) {
            this.timeout(15000);
            testSuiteUtil.testMarkOccurrences("/test006.raml", done);
        });

        it("test007/api.raml", function (done) {
            this.timeout(15000);
            testSuiteUtil.testMarkOccurrences("/test007.raml", done);
        });

        it("test008/api.raml", function (done) {
            this.timeout(15000);
            testSuiteUtil.testMarkOccurrences("/test008.raml", done);
        });

        // TODO uncomment when https://www.mulesoft.org/jira/browse/ALS-220 is fixed.
        // it("test009/api.raml", function (done) {
        //     this.timeout(15000);
        //     testSuiteUtil.testMarkOccurrences("/test009.raml", done);
        // });

        it("test010/api.raml", function (done) {
            this.timeout(15000);
            testSuiteUtil.testMarkOccurrences("/test010.raml", done);
        });

        // TODO uncomment when https://www.mulesoft.org/jira/browse/ALS-221 is fixed.
        // it("test011/api.raml", function (done) {
        //     this.timeout(15000);
        //     testSuiteUtil.testMarkOccurrences("/test011.raml", done);
        // });
        //
        // it("test012/api.raml", function (done) {
        //     this.timeout(15000);
        //     testSuiteUtil.testMarkOccurrences("/test012.raml", done);
        // });
        //
        // it("test013/api.raml", function (done) {
        //     this.timeout(15000);
        //     testSuiteUtil.testMarkOccurrences("/test013.raml", done);
        // });
        //
        // it("test014/api.raml", function (done) {
        //     this.timeout(15000);
        //     testSuiteUtil.testMarkOccurrences("/test014.raml", done);
        // });

        it("test015/api.raml", function (done) {
            this.timeout(15000);
            testSuiteUtil.testMarkOccurrences("/test015.raml", done);
        });

        it("test016/api.raml", function (done) {
            this.timeout(15000);
            testSuiteUtil.testMarkOccurrences("/test016.raml", done);
        });

        it("test017/api.raml", function (done) {
            this.timeout(15000);
            testSuiteUtil.testMarkOccurrences("/test017.raml", done);
        });

        it("test018/api.raml", function (done) {
            this.timeout(15000);
            testSuiteUtil.testMarkOccurrences("/test018.raml", done);
        });

        it("test019/api.raml", function (done) {
            this.timeout(15000);
            testSuiteUtil.testMarkOccurrences("/test019.raml", done);
        });

        it("test020/api.raml", function (done) {
            this.timeout(15000);
            testSuiteUtil.testMarkOccurrences("/test020.raml", done);
        });

        // TODO uncomment when https://www.mulesoft.org/jira/browse/ALS-221 is fixed.
        // it("test021/api.raml", function (done) {
        //     this.timeout(15000);
        //     testSuiteUtil.testMarkOccurrences("/test021.raml", done);
        // });
        //
        // it("test022/api.raml", function (done) {
        //     this.timeout(15000);
        //     testSuiteUtil.testMarkOccurrences("/test022.raml", done);
        // });

        it("test023/api.raml", function (done) {
            this.timeout(15000);
            testSuiteUtil.testMarkOccurrences("/test023.raml", done);
        });

        it("test024/api.raml", function (done) {
            this.timeout(15000);
            testSuiteUtil.testMarkOccurrences("/test024.raml", done);
        });

        it("test025/api.raml", function (done) {
            this.timeout(15000);
            testSuiteUtil.testMarkOccurrences("/test025.raml", done);
        });

        it("test026/api.raml", function (done) {
            this.timeout(15000);
            testSuiteUtil.testMarkOccurrences("/test026.raml", done);
        });
        /* TODO Uncomment after fixing https://www.mulesoft.org/jira/browse/ALS-235
         it("test027/api.raml", function (done) {
         this.timeout(15000);
         testSuiteUtil.testMarkOccurrences("/test027.raml", done);
         });
         */
        /* TODO Uncomment after fixing https://www.mulesoft.org/jira/browse/ALS-155
         it("test028/api.raml", function (done) {
         this.timeout(15000);
         testSuiteUtil.testMarkOccurrences("/test028.raml", done);
         });
         */
        it("test029/api.raml", function (done) {
            this.timeout(15000);
            testSuiteUtil.testMarkOccurrences("/test029.raml", done);
        });

        it("test030/api.raml", function (done) {
            this.timeout(15000);
            testSuiteUtil.testMarkOccurrences("/test030.raml", done);
        });

        it("test031/api.raml", function (done) {
            this.timeout(15000);
            testSuiteUtil.testMarkOccurrences("/test031.raml", done);
        });

        it("test032/api.raml", function (done) {
            this.timeout(15000);
            testSuiteUtil.testMarkOccurrences("/test032.raml", done);
        });

        it("test033/api.raml", function (done) {
            this.timeout(15000);
            testSuiteUtil.testMarkOccurrences("/test033.raml", done);
        });

        it("test034/api.raml", function (done) {
            this.timeout(15000);
            testSuiteUtil.testMarkOccurrences("/test034.raml", done);
        });

        it("test035/api.raml", function (done) {
            this.timeout(15000);
            testSuiteUtil.testMarkOccurrences("/test035.raml", done);
        });

        it("test036/api.raml", function (done) {
            this.timeout(15000);
            testSuiteUtil.testMarkOccurrences("/test036.raml", done);
        });

        it("test037/api.raml", function (done) {
            this.timeout(15000);
            testSuiteUtil.testMarkOccurrences("/test037.raml", done);
        });

        it("test038/api.raml", function (done) {
            this.timeout(15000);
            testSuiteUtil.testMarkOccurrences("/test038.raml", done);
        });

        it("test039/api.raml", function (done) {
            this.timeout(15000);
            testSuiteUtil.testMarkOccurrences("/test039.raml", done);
        });

        it("test040/api.raml", function (done) {
            this.timeout(15000);
            testSuiteUtil.testMarkOccurrences("/test040.raml", done);
        });

        it("test041/api.raml", function (done) {
            this.timeout(15000);
            testSuiteUtil.testMarkOccurrences("/test041.raml", done);
        });

        it("test042/api.raml", function (done) {
            this.timeout(15000);
            testSuiteUtil.testMarkOccurrences("/test042.raml", done);
        });

        it("test043/api.raml", function (done) {
            this.timeout(15000);
            testSuiteUtil.testMarkOccurrences("/test043.raml", done);
        });

        it("test044/api.raml", function (done) {
            this.timeout(15000);
            testSuiteUtil.testMarkOccurrences("/test044.raml", done);
        });
        /* TODO Uncomment after fixing https://www.mulesoft.org/jira/browse/ALS-236
         it("test045/api.raml", function (done) {
         this.timeout(15000);
         testSuiteUtil.testMarkOccurrences("/test045.raml", done);
         });
         */
        it("test046/api.raml", function (done) {
            this.timeout(15000);
            testSuiteUtil.testMarkOccurrences("/test046.raml", done);
        });

        it("test047/api.raml", function (done) {
            this.timeout(15000);
            testSuiteUtil.testMarkOccurrences("/test047.raml", done);
        });

        it("test048/api.raml", function (done) {
            this.timeout(15000);
            testSuiteUtil.testMarkOccurrences("/test048.raml", done);
        });

        it("test049/api.raml", function (done) {
            this.timeout(15000);
            testSuiteUtil.testMarkOccurrences("/test049.raml", done);
        });
    });

    describe('Rename tests',function(){

        it("test001/api.raml", function (done) {
            this.timeout(15000);
            testSuiteUtil.testRename("/test001.raml", done);
        });

        it("test002/api.raml", function (done) {
            this.timeout(15000);
            testSuiteUtil.testRename("/test002.raml", done);
        });

        it("test003/api.raml", function (done) {
            this.timeout(15000);
            testSuiteUtil.testRename("/test003.raml", done);
        });

        it("test004/api.raml", function (done) {
            this.timeout(15000);
            testSuiteUtil.testRename("/test004.raml", done);
        });

        it("test005/api.raml", function (done) {
            this.timeout(15000);
            testSuiteUtil.testRename("/test005.raml", done);
        });

        it("test006/api.raml", function (done) {
            this.timeout(15000);
            testSuiteUtil.testRename("/test006.raml", done);
        });

        // it("test007/api.raml", function (done) {
        //     this.timeout(15000);
        //     testSuiteUtil.testRename("/test007.raml", done);
        // });
        //
        // it("test008/api.raml", function (done) {
        //     this.timeout(15000);
        //     testSuiteUtil.testRename("/test008.raml", done);
        // });
        //
        // it("test009/api.raml", function (done) {
        //     this.timeout(15000);
        //     testSuiteUtil.testRename("/test009.raml", done);
        // });
        //
        // it("test010/api.raml", function (done) {
        //     this.timeout(15000);
        //     testSuiteUtil.testRename("/test010.raml", done);
        // });

        it("test011/api.raml", function (done) {
            this.timeout(15000);
            testSuiteUtil.testRename("/test011.raml", done);
        });

        it("test012/api.raml", function (done) {
            this.timeout(15000);
            testSuiteUtil.testRename("/test012.raml", done);
        });

        it("test013/api.raml", function (done) {
            this.timeout(15000);
            testSuiteUtil.testRename("/test013.raml", done);
        });

        it("test014/api.raml", function (done) {
            this.timeout(15000);
            testSuiteUtil.testRename("/test014.raml", done);
        });

        // it("test015/api.raml", function (done) {
        //     this.timeout(15000);
        //     testSuiteUtil.testRename("/test015.raml", done);
        // });
        //
        // it("test016/api.raml", function (done) {
        //     this.timeout(15000);
        //     testSuiteUtil.testRename("/test016.raml", done);
        // });

        it("test017/api.raml", function (done) {
            this.timeout(15000);
            testSuiteUtil.testRename("/test017.raml", done);
        });

        // it("test018/api.raml", function (done) {
        //     this.timeout(15000);
        //     testSuiteUtil.testRename("/test018.raml", done);
        // });
        //
        // it("test019/api.raml", function (done) {
        //     this.timeout(15000);
        //     testSuiteUtil.testRename("/test019.raml", done);
        // });
        //
        // it("test020/api.raml", function (done) {
        //     this.timeout(15000);
        //     testSuiteUtil.testRename("/test020.raml", done);
        // });

        it("test021/api.raml", function (done) {
            this.timeout(15000);
            testSuiteUtil.testRename("/test021.raml", done);
        });

        it("test022/api.raml", function (done) {
            this.timeout(15000);
            testSuiteUtil.testRename("/test022.raml", done);
        });

        // it("test023/api.raml", function (done) {
        //     this.timeout(15000);
        //     testSuiteUtil.testRename("/test023.raml", done);
        // });

        it("test024/api.raml", function (done) {
            this.timeout(15000);
            testSuiteUtil.testRename("/test024.raml", done);
        });

        it("test025/api.raml", function (done) {
            this.timeout(15000);
            testSuiteUtil.testRename("/test025.raml", done);
        });

        it("test026/api.raml", function (done) {
            this.timeout(15000);
            testSuiteUtil.testRename("/test026.raml", done);
        });
    });
});